PORT=3001
# MongoDB Atlas (replace with your connection string in .env)
MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
# For production, set to your frontend URL(s), comma-separated if multiple
CORS_ORIGIN=http://localhost:3000
